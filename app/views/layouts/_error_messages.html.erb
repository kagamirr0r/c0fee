<% if model.errors.any? %>
	<% model.errors.full_messages.each do |message| %>
	<script type="text/javascript">
      M.toast({html: '<%= message %>', classes: 'red accent-3'})
  </script>
	<% end %>
<% end %>
